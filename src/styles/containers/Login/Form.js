import styled from 'styled-components';

const Form = styled.form`
  height: 500px;
  width: 500px;
  background-color: rgba(211,211,211,0.5);

  text-align: center;
  margin: auto;
`;

export default Form
