import styled from 'styled-components';

const Form = styled.form`
  height: 500px;
  width: 500px;
  background-color: rgba(211,211,211,0.5);

  position: absolute;
  top:0;
  bottom: 0;
  left: 0;
  right: 0;

  margin: auto;
`;

export default Form
